import{_ as e,X as a,Y as i,a1 as s,a0 as n}from"./framework-68dd73a2.js";const l="/blog/assets/Git_22_1.png",t={},d=n(`<h1 id="git如何合并并删除本地以及远程误创建并提交的master分支到git默认的main分支" tabindex="-1"><a class="header-anchor" href="#git如何合并并删除本地以及远程误创建并提交的master分支到git默认的main分支" aria-hidden="true">#</a> Git如何合并并删除本地以及远程误创建并提交的master分支到Git默认的main分支？</h1><div class="hint-container warning"><p class="hint-container-title">注意</p><p>本博文仅供学术研究和交流参考，严禁将其用于商业用途。如因违规使用产生的任何法律问题，使用者需自行负责。</p></div><h3 id="事件回顾" tabindex="-1"><a class="header-anchor" href="#事件回顾" aria-hidden="true">#</a> 事件回顾</h3><ul><li>由于本人在本地创建了一个项目，将其推送到Git远程仓库后，发现本地的是master远程的是main，这就造成了默认的主分支不一致，故而有了这个需求</li></ul><h4 id="问题1-现在github-上默认没有master分支了吗" tabindex="-1"><a class="header-anchor" href="#问题1-现在github-上默认没有master分支了吗" aria-hidden="true">#</a> 问题1：现在github 上默认没有master分支了吗？</h4><p>GitHub上新创建的仓库默认的分支名仍然是<code>master</code>。但是，GitHub在2020年宣布将默认分支名从<code>master</code>更改为<code>main</code>，以避免使用可能被视为种族歧视的词汇。因此，如果您在2020年之后创建了新的仓库，或者仓库是在GitHub宣布这一变更后被初始化的，那么默认分支名可能是<code>main</code>。</p><p>如果您在推送代码时遇到问题，可能是因为您的本地分支名和远程仓库的默认分支名不一致。您可以检查远程仓库的默认分支名，然后确保您的本地分支名与之匹配。以下是如何检查和设置默认分支名的步骤：</p><ol><li><p><strong>检查默认分支名</strong>：</p><ul><li>登录到您的GitHub仓库页面。</li><li>点击“Settings”（设置）。</li><li>在“Options”（选项）部分，找到“Default branch”（默认分支）设置。</li><li>这里会显示当前的默认分支名，您可以确认它是<code>master</code>还是<code>main</code>。</li></ul></li><li><p><strong>更改默认分支名</strong>：</p><ul><li>如果您想要更改默认分支名，可以在“Default branch”设置中进行更改。</li><li>选择您想要设置为默认的分支，然后点击“Update”（更新）。</li></ul></li><li><p><strong>本地分支与远程分支同步</strong>：</p><ul><li>如果您的本地分支名和远程仓库的默认分支名不一致，您可以通过以下命令来重命名本地分支：<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> branch <span class="token parameter variable">-m</span> master main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>这条命令将本地的<code>master</code>分支重命名为<code>main</code>。</li><li>然后，您可以将更改推送到远程仓库：<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-u</span> origin main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ul></li></ol><p>请注意，更改默认分支名可能会影响您的团队成员和项目的CI/CD流程，因此在进行更改之前，请确保通知所有相关人员，并更新任何相关的配置。</p><h4 id="问题2-为什么本地的git-bash-工具-默认就是master不是main" tabindex="-1"><a class="header-anchor" href="#问题2-为什么本地的git-bash-工具-默认就是master不是main" aria-hidden="true">#</a> 问题2：为什么本地的git bash 工具 默认就是master不是main？</h4><p>Git Bash工具本身并不默认创建<code>master</code>分支，而是遵循您初始化Git仓库时或克隆的远程仓库所使用的默认分支名。自从GitHub宣布将新仓库的默认分支从<code>master</code>更改为<code>main</code>以来，如果您创建了新的Git仓库或克隆了新的GitHub仓库，它们通常会以<code>main</code>作为默认分支名。</p><p>如果您在使用Git Bash时默认分支是<code>master</code>，可能有几个原因：</p><ol><li><p><strong>旧仓库</strong>：您正在操作的可能是较旧的仓库，这些仓库在GitHub更改默认分支名之前就已经存在，因此它们使用的是<code>master</code>作为默认分支。</p></li><li><p><strong>克隆旧仓库</strong>：如果您克隆的是一个在更改默认分支名之前就已经存在的远程仓库，那么克隆下来的仓库也会使用<code>master</code>作为默认分支。</p></li><li><p><strong>本地分支未更新</strong>：如果您的远程仓库已经将默认分支更改为<code>main</code>，但您的本地分支尚未更新，那么您的本地Git Bash仍然会显示<code>master</code>作为当前分支。</p></li><li><p><strong>远程仓库配置</strong>：即使GitHub建议使用<code>main</code>，一些组织可能仍然选择使用<code>master</code>作为默认分支名，以保持与现有工具和工作流程的兼容性。</p></li></ol><p>如果您想要将本地的默认分支更改为<code>main</code>，可以按照以下步骤操作：</p><ol><li><p><strong>创建并切换到<code>main</code>分支</strong>（如果尚未存在）：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> checkout <span class="token parameter variable">-b</span> main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>将<code>main</code>分支推送到远程仓库</strong>：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push <span class="token parameter variable">-u</span> origin main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>删除远程仓库的<code>master</code>分支</strong>（如果需要）：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push origin <span class="token parameter variable">--delete</span> master
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>重命名本地的<code>master</code>分支</strong>（如果需要）：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> branch <span class="token parameter variable">-m</span> master main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p><strong>更新远程仓库的默认分支</strong>（在GitHub仓库的设置中）。</p></li></ol><p>完成这些步骤后，您的本地Git Bash和远程仓库应该会使用<code>main</code>作为默认分支。请确保在执行这些操作之前备份您的代码，以防不小心丢失数据。</p><h3 id="解决问题" tabindex="-1"><a class="header-anchor" href="#解决问题" aria-hidden="true">#</a> 解决问题</h3><ul><li>我是在本地创建了项目，然后初始化之后默认就是master分支，而后关联到Gihub以及Gitee，然后推送至远程仓库，而后在远程仓库中发现主分支为main。。。。。。</li></ul><figure><img src="`+l+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><ol><li>本地创建main分支</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>git checkout -b main
git pull github main
git add .
git commit -m &quot;合并master到min&quot;
git pull github main --allow-unrelated-histories

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol start="2"><li>在Vim中输入提交信息并完成合并</li></ol><ul><li><p><strong>输入提交信息</strong>：</p><ul><li>在Vim中，按<code>i</code>键进入插入模式，开始输入您的提交信息。这将解释为什么合并是必要的，特别是如果它合并了更新的上游到一个主题分支。</li></ul></li><li><p><strong>保存并退出</strong>：</p><ul><li>输入完毕后，按<code>Esc</code>键退出插入模式。</li><li>输入<code>:wq</code>（表示写入并退出）然后按<code>Enter</code>键保存提交信息并退出Vim编辑器。</li></ul></li></ul><p>如果您不熟悉Vim，可能会觉得这些步骤有些复杂。您也可以使用以下命令来直接完成合并，而不编辑提交信息：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> merge <span class="token parameter variable">-m</span> <span class="token string">&quot;Merge branch &#39;main&#39; of github.com:wechartApplet/marathonInvertedTriangle&quot;</span> github/main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这个命令中的<code>-m</code>选项允许您直接提供一个提交信息，从而避免了Vim编辑器的启动。</p><p>如果您不想合并远程分支的历史，而是想用远程分支的历史覆盖本地分支（这将丢失本地分支的历史），您可以使用以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> reset <span class="token parameter variable">--hard</span> github/main
<span class="token function">git</span> push <span class="token parameter variable">-f</span> github main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>请注意，<code>git reset --hard</code>会重置您的本地分支到远程分支的状态，这将丢失所有本地未提交的更改。<code>git push -f</code>会强制推送这些更改到远程仓库。</p><p>在执行这些操作之前，请确保您已经备份了所有重要的本地更改，以避免数据丢失。</p><ol start="3"><li>提交到远程的main分支</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>git push github main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="4"><li>删除远程和本地的master分支</li></ol><p>删除远程仓库的<code>master</code>分支</p><ul><li>GitHub:</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push github <span class="token parameter variable">--delete</span> master
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>Gitee:</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> push gitee <span class="token parameter variable">--delete</span> master
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这些命令会将远程仓库中的<code>master</code>分支删除。<code>--delete</code>选项告诉Git您想要删除指定的远程分支。</p><ul><li>删除本地的<code>master</code>分支</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> branch <span class="token parameter variable">-d</span> master
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这个命令会删除本地的<code>master</code>分支。<code>-d</code>选项是<code>--delete</code>的简写，它会删除分支，即使分支不完全合并也没关系。</p><ul><li>检查远程仓库的分支</li></ul><p>为了确认<code>master</code>分支是否已经被删除，您可以使用以下命令来检查远程仓库的分支列表：</p><ul><li>GitHub:</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> fetch github
<span class="token function">git</span> branch <span class="token parameter variable">-r</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>Gitee:</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> fetch gitee
<span class="token function">git</span> branch <span class="token parameter variable">-r</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>这些命令会显示所有远程分支的列表，您可以检查<code>master</code>分支是否还在列表中。</p><ul><li>注意事项</li></ul><ul><li>在删除分支之前，请确保您不再需要该分支上的任何提交，因为删除分支是不可逆的操作。</li><li>如果您在删除分支时遇到问题，可能是因为其他协作者正在使用该分支，或者您需要强制删除。如果是这种情况，您可以使用<code>-D</code>选项代替<code>-d</code>选项来强制删除本地分支，或者使用<code>--force</code>选项来强制推送到远程仓库。</li><li>请确保您在执行这些操作之前已经备份了所有重要的数据。</li></ul><p>按照上述步骤操作后，您应该能够成功地删除Gitee和GitHub的<code>master</code>分支以及本地的<code>master</code>分支。</p><h3 id="如何规避此类问题" tabindex="-1"><a class="header-anchor" href="#如何规避此类问题" aria-hidden="true">#</a> 如何规避此类问题</h3><ol><li>在创建本地项目执行初始化后执行以下命令</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>git branch -m master main   //重命名本地master分支为main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="2"><li>而后再进行关联远程仓库进行pull和push操作</li></ol><hr><h3 id="分割线" tabindex="-1"><a class="header-anchor" href="#分割线" aria-hidden="true">#</a> 分割线</h3><hr><div class="hint-container info"><p class="hint-container-title">相关信息</p><p><strong>以上就是我关于 <em>Git如何合并并删除本地以及远程误创建并提交的master分支到Git默认的main分支？</em> 知识点的整理与总结的全部内容，希望对你有帮助。。。。。。。</strong></p></div>`,60);function r(c,o){return a(),i("div",null,[s(" more "),d])}const p=e(t,[["render",r],["__file","git22.html.vue"]]);export{p as default};
